// Buttons
const toggleBtn = document.getElementById("toggle");

// Event Listeners
toggleBtn.addEventListener("click", toggleLeaderboards);

// Functions
async function toggleLeaderboards(e) {
	const currentMode = e.target.getAttribute("toggle-value");
	if (currentMode) {
		// TODO
	} else {
		// TODO
	}
}
